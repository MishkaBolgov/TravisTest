language: android

branches:
  only:
    - dev

android:
  components:
    - build-tools-28.0.3
    - android-28

script:
    - printf "user=$user\napikey=$apikey" > local.properties
    - echo "now execute"
    - ./gradlew install
    - ./gradlew # mytask